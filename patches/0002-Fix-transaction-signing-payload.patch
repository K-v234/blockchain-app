From 8a14e6d21f3a5bacbfe5b11f7e00fb9d93a54f2a Mon Sep 17 00:00:00 2001
From: Codex <codex@openai.com>
Date: Wed, 4 Feb 2026 14:07:53 +0000
Subject: [PATCH 2/2] Fix transaction signing payload

---
 app.py                   | 18 ++++++++++++------
 templates/index.html     | 28 +++++++++++++++++++++++++---
 tests/test_signatures.py | 16 ++++++++++------
 3 files changed, 47 insertions(+), 15 deletions(-)

diff --git a/app.py b/app.py
index 6f79f0c..4af4aa3 100644
--- a/app.py
+++ b/app.py
@@ -49,17 +49,23 @@ def pubkey_to_address(pubkey_bytes):
     checksum = hashlib.sha256(hashlib.sha256(payload).digest()).digest()[:4]
     return base58.b58encode(payload + checksum).decode()
 
-def signature_message(txid, vout):
-    return f"{txid}:{vout}".encode()
-
-def verify_utxo_signature(vin, utxo):
+def signature_message(tx):
+    sanitized_vin = [{"txid": vin["txid"], "vout": vin["vout"]} for vin in tx.vin]
+    payload = {
+        "vin": sanitized_vin,
+        "vout": tx.vout,
+        "timestamp": tx.timestamp
+    }
+    return json.dumps(payload, sort_keys=True).encode()
+
+def verify_utxo_signature(tx, vin, utxo):
     try:
         signature = bytes.fromhex(vin['signature'])
         pubkey_bytes = bytes.fromhex(vin['pubkey'])
         if pubkey_to_address(pubkey_bytes) != utxo['address']:
             return False
         public_key = ec.EllipticCurvePublicKey.from_encoded_point(ec.SECP256K1(), pubkey_bytes)
-        digest = hashlib.sha256(signature_message(vin['txid'], vin['vout'])).digest()
+        digest = hashlib.sha256(signature_message(tx)).digest()
         public_key.verify(signature, digest, ec.ECDSA(asym_utils.Prehashed(hashes.SHA256())))
         return True
     except Exception:
@@ -188,7 +194,7 @@ class Blockchain:
             utxo = self.utxos.get(utxo_key)
             if not utxo:
                 return False, "referenced utxo not found"
-            if not verify_utxo_signature(vin, utxo):
+            if not verify_utxo_signature(tx, vin, utxo):
                 return False, "invalid signature"
         return True, None
 
diff --git a/templates/index.html b/templates/index.html
index a86c070..f35b69a 100644
--- a/templates/index.html
+++ b/templates/index.html
@@ -71,6 +71,19 @@
     const socket = io();
     let currentAddress = null;
 
+    function canonicalize(value) {
+      if (Array.isArray(value)) {
+        return value.map(canonicalize);
+      }
+      if (value && typeof value === "object") {
+        return Object.keys(value).sort().reduce((acc, key) => {
+          acc[key] = canonicalize(value[key]);
+          return acc;
+        }, {});
+      }
+      return value;
+    }
+
     document.getElementById("createWalletBtn").onclick = async () => {
       const res = await fetch("/wallet/new");
       const data = await res.json();
@@ -112,8 +125,16 @@
       const utxo = utxos[0];
 
       const key = ec.keyFromPrivate(privKey, "pem");
-      const txMsg = `${utxo.txid}:${utxo.vout}`;
-      const msgBytes = new TextEncoder().encode(txMsg);
+      const timestamp = Math.floor(Date.now() / 1000);
+      const payload = {
+        timestamp,
+        vin: [{ txid: utxo.txid, vout: utxo.vout }],
+        vout: [
+          { address: recipient, amount },
+          { address: currentAddress, amount: utxo.amount - amount }
+        ]
+      };
+      const msgBytes = new TextEncoder().encode(JSON.stringify(canonicalize(payload)));
       const digest = await crypto.subtle.digest("SHA-256", msgBytes);
       const hashHex = Array.from(new Uint8Array(digest))
         .map(b => b.toString(16).padStart(2, "0"))
@@ -127,7 +148,8 @@
         vout: [
           { amount, address: recipient },
           { amount: utxo.amount - amount, address: currentAddress }
-        ]
+        ],
+        timestamp
       };
 
       const res = await fetch("/transaction/send", {
diff --git a/tests/test_signatures.py b/tests/test_signatures.py
index adb9862..c995bed 100644
--- a/tests/test_signatures.py
+++ b/tests/test_signatures.py
@@ -7,8 +7,8 @@ from cryptography.hazmat.primitives import hashes
 from app import Blockchain, Transaction, Wallet, signature_message
 
 
-def sign_input(private_key, txid, vout):
-    digest = hashlib.sha256(signature_message(txid, vout)).digest()
+def sign_input(private_key, tx):
+    digest = hashlib.sha256(signature_message(tx)).digest()
     signature = private_key.sign(digest, ec.ECDSA(asym_utils.Prehashed(hashes.SHA256())))
     return signature.hex()
 
@@ -25,15 +25,17 @@ class TransactionSignatureTests(unittest.TestCase):
         utxo_key = f"{txid}:0"
         self.blockchain.utxos[utxo_key] = {"amount": 10, "address": sender.get_address()}
 
-        signature = sign_input(sender.private_key, txid, 0)
         vin = [{
             "txid": txid,
             "vout": 0,
-            "signature": signature,
+            "signature": "",
             "pubkey": sender.serialize_public_key().hex()
         }]
         vout = [{"amount": 5, "address": recipient.get_address()}]
         tx = Transaction(vin, vout)
+        signature = sign_input(sender.private_key, tx)
+        tx.vin[0]["signature"] = signature
+        tx.txid = tx.calculate_txid()
 
         is_valid, error = self.blockchain.validate_transaction(tx)
         self.assertTrue(is_valid)
@@ -47,15 +49,17 @@ class TransactionSignatureTests(unittest.TestCase):
         utxo_key = f"{txid}:0"
         self.blockchain.utxos[utxo_key] = {"amount": 10, "address": sender.get_address()}
 
-        signature = sign_input(attacker.private_key, txid, 0)
         vin = [{
             "txid": txid,
             "vout": 0,
-            "signature": signature,
+            "signature": "",
             "pubkey": sender.serialize_public_key().hex()
         }]
         vout = [{"amount": 4, "address": recipient.get_address()}]
         tx = Transaction(vin, vout)
+        signature = sign_input(attacker.private_key, tx)
+        tx.vin[0]["signature"] = signature
+        tx.txid = tx.calculate_txid()
 
         is_valid, error = self.blockchain.validate_transaction(tx)
         self.assertFalse(is_valid)
-- 
2.43.0
